apiVersion: v1
kind: Service
metadata:
  name: gitlab
  labels:
    app: gitlab
    release: gitlab
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    app: gitlab
    release: gitlab
